# TurboUIGen — Python dependencies
# Install: pip install -r requirements.txt

# Core API server
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0

# LLM integration
openai>=1.14.0
httpx>=0.27.0

# Environment
python-dotenv>=1.0.0

# Figma + QA agents (Playwright for screenshots and browser testing)
playwright>=1.43.0

# Figma MCP server
websockets>=12.0

# Generated app API servers (DataChat: PDF + Excel parsing)
openpyxl>=3.1.2
PyMuPDF>=1.24.0

# Agent config
pyyaml>=6.0.1

# AWS relay (optional — only needed for Bedrock-based LLM routing)
# boto3>=1.34.0

duckdb>=1.0  # in-process dataset MCP tools (dataset_mcp.py)
