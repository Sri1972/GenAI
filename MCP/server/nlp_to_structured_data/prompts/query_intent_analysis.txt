Convert the following natural language query into structured data operations:

USER QUERY: "{query}"

AVAILABLE DATA:
- Columns: {columns}
- Sample Data: {sample_data}
- Business Context: {business_context}

ANALYSIS REQUIREMENTS:
1. **Intent Classification**: Determine the primary intent (filter, aggregate, search, analyze, compare)
2. **Column Mapping**: Map query terms to actual column names using business context
3. **Operation Planning**: Define the sequence of data operations needed
4. **Filter Conditions**: Extract any filtering criteria with operators and values
5. **Aggregation Logic**: Identify grouping and calculation requirements
6. **Output Format**: Determine the best presentation format for results

RESPONSE FORMAT:
```json
{
  "intent": {
    "primary_action": "filter|aggregate|search|analyze|compare",
    "confidence": 0.0-1.0,
    "description": "Brief description of what user wants"
  },
  "columns": {
    "target_columns": ["list", "of", "relevant", "columns"],
    "mapped_terms": {"user_term": "actual_column_name"}
  },
  "operations": {
    "filters": [
      {
        "column": "column_name",
        "operator": "==|!=|>|>=|<|<=|contains|in",
        "value": "filter_value"
      }
    ],
    "aggregations": [
      {
        "column": "column_name", 
        "function": "sum|mean|count|max|min|std",
        "group_by": ["grouping", "columns"]
      }
    ],
    "sorting": {
      "column": "sort_column",
      "direction": "asc|desc"
    }
  },
  "output_preferences": {
    "format": "table|list|summary|chart",
    "limit": number_of_rows,
    "include_totals": true|false
  }
}
```

Focus on accuracy and business relevance in your interpretation.